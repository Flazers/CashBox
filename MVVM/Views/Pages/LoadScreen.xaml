<UserControl x:Class="Cashbox.MVVM.Views.Pages.LoadScreen"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Cashbox.MVVM.Views.Pages"
      xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="64"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Width="64" HorizontalAlignment="Center">
            <Icon:PackIconMaterial Kind="Loading" RenderTransformOrigin="1,1" Width="32" Height="32">
                <Icon:PackIconMaterial.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform/>
                        <RotateTransform x:Name="angle"/>
                        <TranslateTransform/>
                    </TransformGroup>
                </Icon:PackIconMaterial.RenderTransform>
            </Icon:PackIconMaterial>
        </Grid>
        <TextBlock Grid.Row="1" Margin="0 10 0 0" Style="{StaticResource TitleText}" Text="Загрузка приложения..."/>
    </Grid>
</UserControl>
